# Skill 02 · Evidence Judge

每条候选必须拆：FACT / INFERENCE / ASSUMPTION / UNKNOWN。
必须主动问：有什么现实证据能证明这条是错的？

## 评分
价值分记为 **V**（满分 100）。档位只由 V 决定。H 不能把一条拉进 P0。

- 与当前项目直接相关 25
- 证据强度 25（含原算法 Q5「能否追到一手来源」，由宿主模型打，不再单独问 Jev）
- 可执行性 20
- 商业价值 20（含旧过帖问里的收入 / 资产）
- 新颖/非对称信息 10（含原算法 Q7 中文受众、Q9 原帖是否已把答案讲完，由宿主模型打）

85–100 P0；70–84 P1；55–69 WATCH；<55 DISCARD。

**高 P1 = V≥80。** V 70–79 仍是 P1，只出内部卡（信号、V、未知项、为何不进 Converter），**不进 Content Converter**。

每天最多交用户：P0 2 条，P1 3 条。只有 P0 或 V≥80 的 P1 进 Content Converter。

## 盲评
打分输入不含互动指标（浏览、点赞、转发、回复、引用、书签、`public_metrics`）和 H。第三层五题的结果只读 `x-ops` 上的 JSONL 或当次 HOST 记录，不在这里重问 Q1/Q2/Q3/Q4/Q8。

H 在 V 打完之后再拼上，只用于同档排序和 `HOT`（H≥80）。HOT 不改档位。H 的权重在 `Self-Optimizer.md` 黄区，这里不改数字。

## 模型
- 五维打分和原 Q5/Q7/Q9：宿主模型。
- 第三层五题：已经在 Radar 侧按 `skills/jev-x-layer3.md` 问过。这里引用，不重跑。
- 产出必须写明：哪些步用了 Jev，哪些步用了宿主模型。上游整次 `NOT_RUN` 时写「第三层未调用 Jev」。

## 禁止当事实
点赞高、工具新、有人声称 MRR、无来源截图、纯模型跑分无现实意义。
